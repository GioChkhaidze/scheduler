                                                    ICPC 2026 Online Challenge 1 powered by Huawei

        A. Edge–Cloud Collaborative Scheduling                                                 The order of request ids within a group has no semantic effect: legality,
                                                                                               durations, transfer sizes, and all subsequent events depend only on the
                             15 seconds, 256 megabytes                                         member set (D PRE -1 3 4 7 9 and D PRE -1 3 9 4 7 denote the same
                                                                                               task). Order is preserved on echo: TDN repeats the same fields in
This is an interactive scheduling problem. The "interactor" sends events
                                                                                               canonical decimal form with single spaces, and every XDN lists its request
as they happen; after each event group, you choose ready tasks to start.
                                                                                               ids in the order of the triggering task's specification — a D PRE group
You never predict future requests. For a fixed test it is deterministic and
                                                                                               spanning remote computers has per-remote computer local-to-remote
non-adaptive: the same legal responses produce the same events and
                                                                                               transfer lists that remote computer's members as a subsequence of your
score.
                                                                                               D PRE line, an output-step remote-to-local transfer lists the triggering D
IMPORTANT: YES, THERE WILL BE A SYSTEM TEST AFTER THE                                          PROC group in your order.
CHALLENGE.
                                                                                               Dependency Summary
No AI Background Is Needed
                                                                                               Each task is listed separately so its predecessor and completion effect are
Treat this as jobs moving through computers; no text-generation                                easy to scan. For a group, the predecessor rule applies to every member.
knowledge is needed. A token is one output unit. Commands P PRE/P
PROC/P POST prepare a request once; D PRE/D PROC/D POST                                             P PRE — Server: local computer. Requires: the request's ARR
produce one token. Both follow local → remote → local.                                              event. Completes: fixes the request's remote computer and queues
                                                                                                    its input-stage local-to-remote transfer.
 Term                       Meaning                                                                 P PROC piece — Server: the assigned remote computer. Requires:
                                                                                                    for the first piece, the input-stage local-to-remote XDN; for a later
 edge / local               the one computer beside users                                           piece, the previous piece's TDN. Completes: only the last piece (
                                                                                                    le = num_layers ) queues the input-stage remote-to-local transfer.
 cloud / remote             one of K worker computers
                                                                                                    P POST — Server: local computer. Requires: the request's input-
 token                      one output unit                                                         stage remote-to-local XDN. Completes: stops TDR and makes the
                                                                                                    request ready for output.
 prefill / input stage      one-time input preparation
                                                                                                    D PRE — Server: local computer. Requires: each member's previous
 decode / output                                                                                    final-step TDN (P POST or its previous D POST), and the request
                            repeated work producing one token
 step                                                                                               must not be finished. Completes: queues one local-to-remote transfer
                                                                                                    per remote computer represented in the group, in increasing remote-
 uplink / UP                local-to-remote transfer
                                                                                                    computer index.
 downlink / DOWN            remote-to-local transfer                                                D PROC — Server: the assigned remote computer. Requires: the
                                                                                                    local-to-remote XDN carrying each member's current-iteration data.
 FIFO queue                 first transfer queued is the first transfer completed
                                                                                                    Completes: queues one remote-to-local transfer whose length is the
 batch / group              requests combined in one output task                                    group size.
                                                                                                    D POST — Server: local computer. Requires: the remote-to-local
 chunk / piece              a consecutive range of input-stage parts
                                                                                                    XDN carrying each member's current-iteration result. Completes:
                            transferred request data; its size is defined by the                    produces one token per member; after iteration Lout [i], produces
 activations
                            protocol                                                                FIN.

 model layers /                                                                                Interaction
                            the numbered input-stage range [0, num_layers)
 parts                                                                                         Each test is a separate run of your program. Flush the output stream
                                                                                               after every response. In C++, use cout « flush; after printing the
The Problem in One Minute                                                                      complete response; we recommend ios::sync_with_stdio(false);
                                                                                               cin.tie(nullptr); for fast input. In Python, call sys.stdout.flush() after
One local computer and several remote computers handle requests.
                                                                                               printing the complete response (or use print(..., flush=True) for its final
Each request first prepares its input by making this trip:
                                                                                               line).
   local computer ⟶ remote computer ⟶ local computer.                                          The interaction proceeds: startup configuration (2 lines) → Task-Time
                                                                                               Table (N + 1 lines) → repeat {frame → your response} → END. Your
The same trip is then repeated once per token. The first trip is the input
                                                                                               first response follows the first frame. The whole program follows this loop:
stage; each later trip is an output step. Transfers are automatic; you
choose ready tasks for free computers.

The score rewards output rate and short waits. Time to decode ready                            read the 2 parameter lines, then N and the N warmup rows
(TDR) ends when the first output step can begin, before a token is                             loop:
produced. Time per output token (TPOT) is the mean gap between                                        read one line; if it is END: exit
consecutive tokens.                                                                                   parse it as timestamp t; read event count e from the
Challenge Format                                                                               next line
                                                                                                   read the e event lines
Two worked examples appear at the end of the statement. Test 1 is the
                                                                                                   update your state (completions, arrivals, transfers,
public worked Example 1, tests 2–22 are hidden preliminary tests, and
                                                                                               FINs)
finals use a separate frozen set. Per-test scores are reported on the 0–
                                                                                                   choose assignments for currently free resources
1000 scale defined below, and the contest system aggregates them
according to the contest rules; no hacks are used.                                             (possibly none)
                                                                                                    print n and the n assignment lines; flush
                                                                                               For a simple implementation, store one state per request. Its path is



                                                                                               ARR -> P PRE -> input stage UP -> P PROC piece(s) -> input
                                                                                               stage DOWN -> P POST
                                                                                                   -> D PRE -> output UP -> D PROC -> output DOWN -> D
                                                                                               POST
Guide: blue is local, green is the shared two-way link, and orange is                               -> either FIN or the next D PRE
remote; computation and transfer may overlap. Figure labels use the                            Move a request to its next state only when the corresponding event
glossary above.                                                                                appears in a frame. Mark a computer busy when you assign it and free
                                                                                               only when its TDN appears. Tracking these states is enough for a correct
What You Control
                                                                                               solution. You do not need to predict when tasks will finish.
You choose:
                                                                                               You act only when a frame arrives. There is no timer or self-wake
    which legal task to start on each free computer;                                           mechanism: you cannot ask to be woken at a chosen future time, and you
    the remote computer assigned to a request when scheduling its P                            cannot deliberately idle a computer until an arbitrary moment. A task can
    PRE;                                                                                       only be assigned in response to a frame, at that frame's timestamp. If you
                                                                                               delay an action, you must wait for a future event frame.
    how to split an input-stage P PROC into ranges of numbered parts;
    and                                                                                        Mistakes That Give Zero Points on a Test
    which ready requests to combine into each output group.
                                                                                               The following errors score 0 on the test. A legal but slow choice is still
Transfers are automatic. For a first solution, use one full input-stage piece                  valid; it only lowers your score.
and groups of size 1.
                                                                                                    Assigning a task to a busy computer, or two tasks to one resource in a
System Model                                                                                        single response.
                                                                                                    Assigning a task before all of its required earlier events have been
    There is one local computer and K identical remote computers,
                                                                                                    delivered. This includes referencing a rid that has not arrived and re-
    numbered 0 through K − 1 and written C0, C1, …. They work
                                                                                                    issuing a completed step.
    independently and may run while data moves.
                                                                                                    Including a request that is already part of an in-flight task, or that has
    There are R requests in total, but R is not announced. Request ids
                                                                                                    already finished (FIN).
    are i = 0, 1, … , R − 1 in arrival order. The arrival time of request i
                                                                                                    Wrong remote computer: P PROC/P POST with a remote computer
    is the timestamp of its ARR event. That event reveals its input length
                                                                                                    other than the request's assigned remote computer; P PRE with a
          ; its output length Lout [i] remains hidden until a FIN event
    Lin [i]
                                                                                                    remote computer outside [0, K ) ; a D PROC member assigned to a
    reports that it has finished.
                                                                                                    different remote computer.
    Request i performs one token-free input stage, then exactly Lout [i]
                                                                                                    Illegal piece: empty (ls = le), outside [0, num_layers], or not
    output steps, each producing one token. Thus total generated tokens
                                                                                                    ascending and gap-free.
    are ∑i Lout [i] , with Lout [i] ≥ 1 .
                                                                                                    Malformed group: m < 1 or duplicate request ids.
    You assign each request to one remote computer in its P PRE task.
                                                                                                    Malformed or unparsable output.
    The choice is fixed: all later input stage tasks for that request name
                                                                                                    Reaching a stuck state with unfinished requests and no possible
    the same remote computer, and every D PROC containing it runs
                                                                                                    future event (see below).
    there. Output-step tasks on the local computer may combine requests
                                                                                                    Exceeding the time or memory limit.
    from different remote computers.
    Each computer executes at most one task at a time. Once a task                             Errors and stream closure
    starts, it cannot be paused. Separately scheduled input-stage pieces
                                                                                               The interactor never produces -1 as a failure response. This is unrelated
    may be alternated with other work.
                                                                                               to the mandatory -1 marker in participant commands D PRE and D POST.
    Schedule cost S : a task assigned at time t with execution duration
                                                                                               On a protocol violation or malformed response it simply stops: the test
    dur occupies its computer over [t, t + S + dur] . The cost is paid                         scores 0 and no further frames are sent. If reading input ever fails or
    once per task, including once per input-stage piece and once per                           reaches end-of-file, exit immediately with exit code 0 — do not block
    output group. Transfers do not pay S .                                                     waiting for more input and do not crash; the verdict is determined by the
    Execution times are fixed: task duration depends on its step and                           interactor, not by your exit path.
    group size (and fraction of parts for input-stage pieces), never on the
                                                                                               Getting Stuck
    output-token position or saved internal data size. See Input — Task-
    Time Table. Finished tasks and transfers are reported as TDN and                           If unfinished requests remain but no task, transfer, or future arrival can
    XDN.                                                                                       create another event, the run is stuck. The interactor detects this stuck
                                                                                               state, terminates, and assigns 0 to the test. While future arrivals remain,
Request Lifecycle                                                                              responding with 0 assignments is safe because time advances to the next
                                                                                               arrival. Delaying a request hurts the score; permanently abandoning it can
Solid arrows are scheduling dependencies; dashed arrows are automatic
                                                                                               cause this stuck state.
transfers.
                                                                                               Termination

                                                                                               When all requests have finished, the interactor sends END (a single line,
                                                                                               in place of the next frame's header) after reading your response to the
                                                                                               final frame. Read it and exit.

                                                                                               The total number of requests R is not announced in advance and there is
                                                                                               no "no more arrivals" signal — you cannot distinguish a lull from the end
                                                                                               of the stream. This is intentional; plan your grouping accordingly.

                                                                                               Scoring
                                                                                               You do not need to calculate the score to write a valid scheduler. First
                                                                                               make a scheduler that finishes every request legally. Read the formula
                                                                                               only when you are ready to improve its score. It balances two intuitive
                                                                                               goals:
Guide: TDR ends at P POST; each later D POST makes one token.
                                                                                                    Output rate: finish more output tokens per millisecond of simulated
PRE/POST use the local computer; PROC uses the assigned remote;
                                                                                                    time.
transfers use the shared link.
                                                                                                    waiting time: make requests ready for output promptly and avoid long
Input Stage                                                                                         gaps between produced tokens.

Three steps: first (local) → process (assigned remote) → final (local).                        Each goal becomes a component in [0, 1]. The weights wtp and wc say
An input-stage group has one request.                                                          how much that test values each goal, and the final score is their weighted
                                                                                               sum times 1000 .
    P PRE (local computer). May start after: the request's ARR event.
    Fixes the remote computer; on completion, the local-to-remote                              The exact guarantees are: wtp , wc ≥ 0; wtp + wc = 1 ; SLO1 > 0 ;
    transfer is queued immediately (whether or not that remote computer                        SLO2 > 0 ; tpUB > tpbase ; distbase ≥ 0. Either weight may be 0, but

    is busy).                                                                                  every request must still be completed and every input/output rule obeyed.
    P PROC (remote computer) computes all num_layers numbered                                  The following formula turns a value into a number from 0 to 1:
    parts. The simplest choice is one full piece [0, num_layers). You
                                                                                               clamp(x; base, target) = max(0, min(1, (x − base)/(target − base)))
    may split it so other remote work can run between pieces.
    P POST (local computer). May start after: the request's input stage                        (0 at base , 1 at target, clamped outside).
    remote-to-local transfer XDN. Completion makes the request ready for
                                                                                               Output-Rate Component. tp = ∑i Lout [i] / total elapsed time
    producing output; TDR is measured from arrival to this completion.
                                                                                               tokens/ms, where total elapsed time = (the latest final-token production
Exact splitting rules. Each piece is a nonempty integer range [ls, le) : it                    time over all requests) − (the earliest request arrival time), in ms. Output
includes part ls and stops before part le . For each request, pieces must                      rate component = clamp(tp; tpbase , tpUB ). The value tpbase comes
be issued in ascending, gap-free order: the first starts at 0, each later ls                   from a fixed one-request-at-a-time reference schedule; tpUB is an
equals the previous le , and the last ends at num_layers . Thus there are                      estimated high rate. At or below the baseline the component is 0; at or
at most num_layers pieces, and none when num_layers = 1 beyond                                 above the upper bound it is 1; between them it grows linearly. These
the full piece. The first piece waits for the input stage local-to-remote                      values set scoring and are not promises about any solution in the testing
transfer XDN; each later piece waits for the previous piece's TDN. Its                         kit.
duration is                                                                                    Waiting-Time Component. The input gives two waiting-time targets.
                                                                                               SLO1 is the target average wait until a request is ready for its first output
                           le − ls
                                                  × prefill_proc(Lin [i]).                     step (time to decode ready, TDR). No token has been produced at this
                       num_layers
                                                                                               point. SLO2 is the target mean gap between consecutive produced tokens
Only the last piece queues the input stage remote-to-local transfer, of                        (TPOT). The letters SLO are only part of the fixed input names. For
length Lin [i]. No transfer occurs between pieces. Any range that is empty,                    request i , let e1 < e2 < ⋯ < eL [i] be its token production times, i.e.
                                                                                                                                     out



outside the part interval, out of order, or leaves a gap is a violation.                       the completion times of its output D POST tasks.

Output Steps                                                                                        tdr =   mean over all requests of (input stage final step completion −
                                                                                                    arrival).
After the input stage, request i performs Lout [i] output steps in
                                                                                                    tpot = mean of ej+1 − ej over all consecutive output gaps of all
succession: completing iteration k 's final step readies iteration k + 1. The
same three steps are used, but multiple requests may be grouped per                                 requests pooled (1 ≤ j < Lout [i] ). A request with Lout [i] = 1
step. You choose each group independently. Output-step tasks cannot be                              contributes no gaps. If no request contributes a gap, tpot is defined
split into part ranges.                                                                             as 0.

    D PRE (local computer, across remote computers): may group                                                             tdr − SLO1                                            tpot − SLO2
                                                                                               excess_tdr = max(0,                             ),       excess_tpot = max(0,                   ),
    requests assigned to any mix of remote computers — the listed                                                              SLO1                                                SLO2

    decode_pre time depends only on total group size, so across remote                                                      −−−−−−−−−−−−−−−−−−−−−
                                                                                                                                           2                      2
                                                                                                                dist = √ excess_tdr            + excess_tpot .
    computers grouping improves local computer efficiency (the task-time
    table plus the transfer formula is the only efficiency model; nothing
                                                                                               waiting-time component = clamp(dist; distbase , 0) — the same
    else rewards grouping). May start after, for each request: completion
                                                                                               conversion with lower values treated as better: lower dist is better, 1 at
    of that request's previous final step (P POST for its first output step,
                                                                                               dist = 0 , 0 at the reference scheduler's amount above the waiting-time
    otherwise its previous D POST) — and the request must not be                               targets distbase . Written out directly (the form easiest to implement and
    finished (FIN always arrives with the final D POST TDN, so you                             verify):
    always know). Completion queues one local-to-remote transfer per
    distinct remote computer in the group (len = that remote computer's                                                             ⎧ max(0, 1 − dist/distbase )          if distbase > 0,

    member count), enqueued simultaneously in increasing remote                                waiting-time component           =   ⎨1                                    if distbase = 0 and dist = 0,
                                                                                                                                    ⎩
    computer index order.                                                                                                             0                                   if distbase = 0 and dist > 0.

    D PROC (remote computer): all members must be assigned to that
                                                                                               Important special case: if distbase = 0, the waiting-time component is
    remote computer. May start after, for each request: the local-to-remote
                                                                                               either 0 or 1. It is 1 only when both mean-waiting-time targets are met (
    transfer XDN carrying that member's current-iteration data — the rule
                                                                                               dist = 0 ), and 0 otherwise.
    is checked separately for each request: members may come from
    different D PRE groups and different local-to-remote transfers.
    Completion queues one remote-to-local transfer (len = this group's
    size).
    D POST (local computer, across remote computers). May start after,
    for each request: the remote-to-local transfer XDN carrying that
    member's current-iteration result (different D PROC groups/remote-to-
    local transfers are fine). Each member's completion produces one
    token.


                                                                                               Diagram guide: the upper branch rewards higher output rate; the lower
                                                                                               branch rewards smaller normalized TDR/TPOT target violations. The two
                                                                                               [0, 1] components are weighted, added, and multiplied by 1000 .


                                                                                               NormalizedScore = wtp ⋅ clamp(tp; tpbase , tpUB ) + wc ⋅ clamp(dist; distbase , 0),

                                                                                               Score = 1000 ⋅ NormalizedScore.


                                                                                               Thus each completed test awards a score in [0, 1000]. Verdicts. A
                                                                                               completed interaction scores as above. A protocol error, malformed
                                                                                               output, stuck state with no future event, or exceeding the time/memory
                                                                                               limit scores 0 on that test; other tests are unaffected. No partial credit is
Guide: the upper lane splits one input stage into consecutive ranges; the                      awarded for an unfinished test.
lower lane groups ready requests for output.                                                   Contest aggregation. The 22 preliminary tests provide feedback and do
Groups: m ≥ 1, request ids distinct, every member satisfying its                               not contribute to the final ranking. The final score is the arithmetic mean of
predecessor rule; m = 1 is always allowed. Only output work is ever                            the 20 frozen final-test scores. Ranking uses that mean before display
grouped — input-stage groups always contain exactly one request. There                         rounding; the displayed score is rounded to three digits after the decimal
is no other group-size limit: no maximum exists or is announced — any                          point. Therefore a known per-test vector of 0, 500, and 1000 -point
nonempty set of distinct, currently ready requests may be grouped,                             outcomes contributes exactly those values before averaging. This problem
subject only to the step and remote computer rules above. Order of                             is not open to hacks.
request ids within a group: see Output.
                                                                                               input
Input                                                                                          1 1.000000000 2.000000000 1.000000000 125000 4
Protocol Guide                                                                                 30.000000000 15.000000000 0.062500000 0.022222222 0.000000000
                                                                                               0.500000000 0.500000000
P is the one-time input stage; D is one output step. PRE/POST are local                        2
tasks and PROC is remote. Events are ARR (arrival), TDN (task done),                           1 3.000000000 10.000000000 2.000000000 1.000000000 4.000000000
XDN (transfer done), and FIN (finished). In [ls, le), ls is included and le                    1.000000000
                                                                                               4 3.000000000 10.000000000 2.000000000 1.000000000 4.000000000
excluded. Input arrives interactively: read each whole frame and respond
                                                                                               1.000000000
once. Times are real milliseconds; counts, lengths, and ids are integers;                      0.000000000
fields use single spaces.                                                                      1
                                                                                               ARR 0 4
Startup Configuration                                                                          4.000000000
                                                                                               1
The interactor first sends two lines (no response expected): the system                        TDN E P PRE 0 0 3.000000000
parameters, then the scoring parameters:                                                       10.000000000
                                                                                               1
                                                                                               XDN UP 0 500000 PRE 1 0
                                                                                               21.000000000
K S latency_in_ms bandwidth_gbps bytes_per_token num_layers                                    1
SLO1 SLO2 tp_UB tp_base dist_base w_tp w_c                                                     TDN C0 P PROC 0 4 0 0 10.000000000
                                                                                               27.000000000
K , bytes_per_token , and num_layers are integers; all other values
                                                                                               1
are reals (times in ms, output rates in tokens/ms). SLO stands for service-                    XDN DOWN 0 500000 PRE 1 0
level objective: SLO1 is the TDR target and SLO2 is the TPOT target. The                       30.000000000
other names on the scoring line mean: tp_UB = tpUB , tp_base                                   1
                                                                                               TDN E P POST 0 0 2.000000000
= tpbase , dist_base = distbase , w_tp = wtp , w_c = wc .
                                                                                               32.000000000
bandwidth_gbps is in gigabits per second (Gb/s). The value                                     1
bytes_per_token is already the complete data size for one token; do not                        TDN E D PRE -1 1 0 1.000000000
multiply it by an element width.                                                               35.000000000
                                                                                               1
Communication                                                                                  XDN UP 0 125000 DEC 1 0
                                                                                               40.000000000
Transfers are automatic: the interactor queues them when their triggering                      1
                                                                                               TDN C0 D PROC 0 1 0 4.000000000
computation finishes; you never output a transfer command. A single link
                                                                                               43.000000000
connects the local computer and remote computer side and is shared by                          1
all remote computers. UP means a local-to-remote transfer (local                               XDN DOWN 0 125000 DEC 1 0
computer → remote computer), and DOWN means a remote-to-local                                  45.000000000
                                                                                               2
transfer (remote computer → local computer). These are independent
                                                                                               TDN E D POST -1 1 0 1.000000000
one-at-a-time FIFO queues: transfers finish in the order they enter, and                       FIN 0
both directions may be active simultaneously. For simultaneous queues,                         END
one D PRE's per-remote transfers enter by increasing remote index.
                                                                                               output
Otherwise transfers enter in interactor event order; tasks started together
and finishing together use assignment-line order.                                              1
                                                                                               E P PRE 0 0
Transfer time                                                                                  0
                                                                                           6   1
= latency_in_ms + 8 data_bytes/(bandwidth_gbps × 10 )                      ms,                 C0 P PROC 0 4 0 0
where data_bytes = len × bytes_per_token. The factor 8 converts                                0
bytes to bits. For an input-stage transfer of request i , len = Lin [i] ; for                  1
output, use the per-remote computer local-to-remote transfer and per-                          E P POST 0 0
                                                                                               1
group remote-to-local transfer sizes defined in Output Steps. Guaranteed
                                                                                               E D PRE -1 1 0
latency_in_ms > 0 : every transfer takes strictly positive time.                               0
                                                                                               1
Task-Time Table                                                                                C0 D PROC 0 1 0
                                                                                               0
Before requests arrive, the judge gives you a table telling you how long                       1
tasks take, in milliseconds. You only read this table; you do not measure                      E D POST -1 1 0
the times yourself. Read an integer N , then N rows of 7 values; no                            0
response is expected:
                                                                                               Examples

                                                                                               If interactive problems are new to you, begin with Example 1 and focus on
batch_size prefill_pre prefill_proc prefill_post                                               the repeating pattern: read a complete frame on the left, update your
decode_pre decode_proc decode_post                                                             state, then print one complete response on the right. The exact
Here batch_size means the number of requests grouped into one task.                            timestamps are consequences of the supplied costs; your scheduler never
The names containing prefill refer to the input stage, and the names                           has to predict them. The transcript below shows the participant-side input
containing decode refer to output steps. These fixed names come from                           and output directly; output responses contain no timestamps.
the protocol; no AI knowledge is required. The values help you compare
                                                                                               The examples use S =1 , latency_in_ms=2 , bandwidth_gbps=1 ,
scheduling choices. You must read all rows, but a first correct scheduler
                                                                                               and bytes_per_token=125000. Because bandwidth is in gigabits per
does not need to use the values at all; a more advanced scheduler will                                                                                            6
                                                                                               second, a one-token transfer takes 2 + 8 ⋅ 125000/10 = 3 ms. Every
use the complete task-time table, including values between listed rows.
                                                                                               table has the interactor's input on the left and your complete response on
batch_size means Lin for the three input stage columns (one request per                        the right. Each protocol line occupies its own table row. A response begins
input stage group) and member count for the three output columns. The                          with its assignment count.
batch_size values are distinct positive integers in [1, 4096]. A listed
                                                                                               Example 1: one request, end to end.
output-step group size may exceed the test's R ; such a row only defines
the task-time table and does not make that group size schedulable. Not                         Here K =1 , num_layers=4, request 0 has Lin [0] = 4 and
every step is listed at every group size; a missing value is -1. Rows are                      Lout [0] = 1, and the relevant execution durations are 3, 10, 2 ms for
given in no guaranteed order. Guarantee: every step column contains at                         input stage and 1, 4, 1 ms for output. The scheduler uses one full input-
least one non-missing entry.                                                                   stage piece.
All listed values are execution durations excluding the schedule cost
S , exactly like the dur field of every TDN: the total time for which a
                                                                                                Interactor                                          Your response
computer is busy with a task is always S + dur (see System Model). For
each step, sort the available rows by group size. If the needed size is
                                                                                                0.000000000                                         1
listed, use its time. If it lies between two listed sizes, draw a straight line
between their times and use the value on that line. Below the smallest                          1                                                   E P PRE 0 0
size, use the first time; above the largest size, use the last time. The result
is the same on every remote computer and never changes. Guarantee:                              ARR 0 4
every resulting legal task duration is strictly positive. Here R is the test's
total request count (see Constraints); R is not announced during the
                                                                                                4.000000000                                         0
interaction and is not a separate group-size limit. No maximum output
group size exists beyond the number of currently ready requests. The                            1
duration of every completed task is echoed in its TDN's dur field.
                                                                                                TDN E P PRE 0 0 3.000000000
Event Frames (Your Turns)

The interactor sends a frame whenever one or more events occur. A frame
contains: one timestamp line t, one event-count line e, then e event lines.                     10.000000000                                        1
Always read the whole frame before deciding what to do.
                                                                                                1                                                   C0 P PROC 0 4 0 0
The frame timestamp is its event time, printed with 9 decimal places;
                                                                                                XDN UP 0 500000 PRE 1 0
printed timestamps are nondecreasing, while the internal event times of
consecutive frames are strictly increasing. Only events with exactly the
same internal timestamp are coalesced. A later event is never moved to                          21.000000000                                        0
an earlier frame, and line order carries no scheduling priority. Your
response may use any event in the frame: every resource freed by a TDN                          1
is free, even if that event is the last line. Guarantee: FIN appears beside
                                                                                                TDN C0 P PROC 0 4 0 0 10.000000000
the TDN of that request's final output final step. After reading such a
frame, the request is finished and must not appear in your response to
that frame or any later response.
                                                                                                27.000000000                                        1

                                                                                                1                                                   E P POST 0 0

                                                                                                XDN DOWN 0 500000 PRE 1 0


                                                                                                30.000000000                                        1

                                                                                                1                                                   E D PRE -1 1 0

                                                                                                TDN E P POST 0 0 2.000000000


Guide: read the whole event frame, update state, then print one count and
                                                                                                32.000000000                                        0
exactly that many assignments.
                                                                                                1
    ARR <rid> <Lin [i]> — request i = rid arrived; its arrival time is this
    frame's timestamp t ; Lin [i] is given, while Lout [i] is unknown.                          TDN E D PRE -1 1 0 1.000000000
    TDN <server> <task_spec> <dur> — a task completed and its
    server is now free. task_spec is echoed in a canonical equivalent
                                                                                                35.000000000                                        1
    form: integers use ordinary decimal notation and fields use single
    spaces; dur is the execution duration, excluding S , printed with 9                         1                                                   C0 D PROC 0 1 0
    digits after the decimal point.
                                                                                                XDN UP 0 125000 DEC 1 0
    XDN <UP|DOWN> <remote> <size> <PRE|DEC> <m> <rid...> — a
    transfer completed (its data has arrived). size is in bytes (
    = len × bytes_per_token); PRE/DEC marks an input-stage or
                                                                                                40.000000000                                        0
    output-step transfer; per-remote computer transfers each produce
    their own XDN; for input stage, m = 1 . The m request ids listed are                        1
    exactly the requests whose data the transfer carries: the single
                                                                                                TDN C0 D PROC 0 1 0 4.000000000
    request for input stage; that remote computer's members of the
    triggering D PRE for an output-step local-to-remote transfer; the
    members of the triggering D PROC group for an output-step remote-                           43.000000000                                        1
    to-local transfer.
                                                                                                1                                                   E D POST -1 1 0
    FIN <rid> — the request finished all output steps (its last token was
    just produced). It must not appear in any task you assign from now on.                      XDN DOWN 0 125000 DEC 1 0

<server> = E (local computer) or Ck (remote computer k ); <m> is the
number of request ids that follow. request ids are integers                                     45.000000000                                        0
0, 1, … , R − 1, assigned in arrival order, never reused.

                                                                                                2
Constraints
                                                                                                TDN E D POST -1 1 0 1.000000000
                   ;
    1 ≤ K ≤ 8 1 ≤ R ≤ 2000                          ; 1 ≤ Lin [i] ≤ 4096 ;
                              ;
    1 ≤ Lout [i] ≤ 512 ∑ Lout [i] ≤ 2 ⋅ 10
                                                                   5
                                                                       per test;                FIN 0
                                          i

    1 ≤ num_layers ≤ 64 2 ≤ N ≤ 4096          ;                    .
                       ;
    1 ≤ S ≤ 10 0.001 ≤ latency_in_ms ≤ 50                                    ;
                                                                                                END                                                 exit
    0.001 ≤ bandwidth_gbps ≤ 100                            ;
                                                    6
    1 ≤ bytes_per_token ≤ 10                            .                                      The input-stage final step completes at 30.000 , so this request's TDR is
                                                    9                                  9
    0.001 ≤ SLO1, SLO2 ≤ 10                             ; 0 ≤ tpbase , distbase ≤ 10 ;         30 ms. Its only token is produced at 45.000 . Because Lout [0] = 1, it
         −9                       9
    10        ≤ tpUB ≤ 10             ; and tpUB > tpbase .                                    contributes no TPOT gap.
                                                                                 4
    Every non-missing task-table entry is in [0.001, 10 ] ms. System
                                                                                               Example 2: grouping requests from two remote computers.
    reals, task times, timestamps, and durations use 9 decimal places.
    Arrival timestamps are nondecreasing in [0, 10 ] ms. Completion
                                                                         9
                                                                                               Assume requests 0 and 1 have already been assigned to C0 and C1,
                                      9
    frames may exceed 10 , but the validator guarantees a conservative                         respectively, and both are ready for their first output step at time 16.500 .
                            12                                                                 This excerpt shows one across-remote-computers first step group, two
    upper bound of 10 ms even if every legal task and transfer is
                                                                                               single-remote-computer process tasks, and one across-remote-computers
    serialized; all timestamps are nonnegative finite doubles.
                                                                                               final step group.
    You never need to predict event timestamps to keep the protocol
    correct: every completion is announced by the interactor (TDN/XDN)
                                                                                                Interactor                                     Your response
    with its timestamp and duration, so a purely reactive scheduler needs
    no arithmetic on future times. If you simulate ahead for planning, use
    ordinary double-precision arithmetic with the piecewise-linear lookup                       16.500000000                                   1
    and transfer formula above.
                                                                                                1                                              E D PRE -1 2 0 1
    At most 2 ⋅ 106 frames per test; use fast I/O.
    Degenerate values (e.g. K = 1 , num_layers = 1) occur and                                   TDN E P POST 1 1 1.250000000
    simply disable the corresponding mechanic.

Output                                                                                          19.000000000                                   0
After every frame (one turn), print a count n (0 ≤ n ≤ K + 1), followed
by n assignments. For readability the examples put one assignment on                            1
each line, but the parser accepts arbitrary whitespace between tokens.
                                                                                                TDN E D PRE -1 2 0 1 1.500000000
Each assignment has the form <server> <task_spec>. It starts one task
on a computer that is currently free. The command words shown below
are fixed output syntax; copy them exactly. Integer tokens use the ordinary                     22.000000000                                   1
signed-decimal syntax accepted by C++ conversion (an optional leading +
and leading zeros are accepted); echoed integers are canonical decimal.                         1                                              C0 D PROC 0 1 0
Thus a response containing no assignments is simply
                                                                                                XDN UP 0 125000 DEC 1 0


0                                                                                               25.000000000                                   1
For a first scheduler, most responses may contain only zero or one task.
Unmentioned resources stay idle. You may leave any free computer idle                           1                                              C1 D PROC 1 1 1
even when one or more legal tasks are available. Waiting can be a useful
                                                                                                XDN UP 1 125000 DEC 1 1
grouping choice, but do not create the stuck state with no future event
described in Interaction. Every frame is a scheduling opportunity,
including frames containing only transfers or arrivals. After reading the                       27.000000000                                   0
whole frame, you may use any predecessor or free resource reported
anywhere in it.                                                                                 1

When deciding whether a task is legal, ask three questions: Is its                              TDN C0 D PROC 0 1 0 4.000000000
computer free? Has every required predecessor event arrived? Is every
request in the task at exactly this step and not already in flight or finished?
The tables below make these checks precise.                                                     30.000000000                                   0

All assignments in one response begin simultaneously at timestamp t .                           2
One cannot depend on another assignment from that same response.
                                                                                                TDN C1 D PROC 1 1 1 4.000000000
Assign at most one task to each resource; it becomes free again only
when its TDN arrives. The six legal task shapes are:                                            XDN DOWN 0 125000 DEC 1 0

 step                  task_spec                                Server
                                                                                                33.000000000                                   1
 input stage                                local computer (also fixes the
                       P PRE <remote> <rid>
 first step                                 remote computer)                                    1                                              E D POST -1 2 0 1

 input stage           P PROC <ls> <le>                                                         XDN DOWN 1 125000 DEC 1 1
                                                                that remote computer
 process               <remote> <rid>

 input stage           P POST <remote>                                                          35.500000000                                   0
                                                                local computer
 final step            <rid>
                                                                                                3
 output first                                                   local computer (across
                       D PRE -1 <m> <rid...>                                                    TDN E D POST -1 2 0 1 1.500000000
 step                                                           remote computers)
                                                                                                FIN 0
 output                D PROC <remote>
                                                                that remote computer
 process               <m> <rid...>                                                             FIN 1

 output final          D POST -1 <m>                            local computer (across
 step                  <rid...>                                 remote computers)               END                                            exit


For D PRE/D POST, the -1 marks a group spanning remote computers;                              The two requests may share the local computer tasks even though their
for D PROC, all members must be assigned to <remote>. In P PRE,                                process tasks run on different remote computers. The two local-to-remote
<remote> must be in [0, K ) ; in P PROC and P POST, <remote> must                              transfers are handled one at a time, in remote-computer number order;
equal the request's assigned remote computer — anything else is a                              the final D POST combines results delivered by two different remote-to-
violation. (P POST itself runs on the local computer: its <remote> field                       local transfers.
carries no scheduling meaning and is purely a consistency check against
                                                                                               An output task's time depends only on its group size, never on which
the request's fixed assignment.) Iteration indices are never transmitted:
                                                                                               output token is being generated or how many tokens are in the request's
each request's steps are strictly sequential, so your own bookkeeping is
                                                                                               previously stored data. The task-time table and transfer formula are the
the sole — and sufficient — source of truth for which iteration a task
                                                                                               complete cost model.
belongs to.


                                                                  Codeforces (c) Copyright 2010-2026 Mike Mirzayanov
                                                                    The only programming contests Web 2.0 platform
